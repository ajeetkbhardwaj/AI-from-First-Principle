"""
MiniGPT : An auto-regressive(causal LM)
Generative Pre-trained Transformer(GPT) is a decoder only model without encoder. Thus, each decoder layer of GPT-based
model consists of masked self-attention and a feedforward network without encoder-decoder attention.

"""